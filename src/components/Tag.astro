---
import IconHash from "@/assets/icons/IconHash.svg";
import { type Locale, defaultLocale } from "@/i18n/translations";
import { getLocalizedPath } from "@/i18n/utils";

type Props = {
  tag: string;
  tagName: string;
  size?: "sm" | "lg";
  lang?: Locale;
};

const { tag, tagName, size = "lg", lang = defaultLocale } = Astro.props;
---

<li>
  <a
    href={getLocalizedPath(`/tags/${tag}/`, lang)}
    transition:name={tag}
    class:list={[
      "flex items-center gap-0.5",
      "border-b-2 border-dashed border-foreground",
      "hover:-mt-0.5 hover:border-accent hover:text-accent",
      "focus-visible:border-none focus-visible:text-accent",
      { "text-sm": size === "sm" },
      { "text-lg": size === "lg" },
    ]}
  >
    <IconHash
      class:list={[
        "opacity-80",
        { "size-5": size === "lg" },
        { "size-4": size === "sm" },
      ]}
    />
    {tagName}
  </a>
</li>
